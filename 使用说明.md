# 两分钟上手指南

你只需要做三件事：**放数据 → 点两下鼠标 → 看结果。**

---

## 第 0 步：第一次用？先跑这个（以后跳过）

在 VSCode 里打开 `setup_env.py`，右键 → **Run Python File in Terminal**。

- 如果打印 `✓ 环境就绪` → 跳到第 1 步
- 如果打印 `✓ 环境配置完成` → 跳到第 1 步
- 如果报错 → 拉到最下面看"出错了怎么办"

---

## 第 1 步：把数据放进去

把你的行情 CSV 文件全部扔进这个文件夹：

```
data/stock/
```

**两个硬性要求：**

> ⚠️ **文件名必须是 `代码.交易所.csv`**，比如 `000001.SZ.csv`、`600000.SH.csv`
>
> ⚠️ **文件里必须有这 6 列**（一字不差！）：`EventDate`、`money`、`volume`、`high`、`close`、`prev_close`

打开 CSV 看一眼，第一行应该是这样：

| EventDate | money | volume | high | close | prev_close |
|-----------|-------|--------|------|-------|------------|
| 2024-01-02 | 1234567890 | 9876543 | 10.50 | 10.20 | 10.15 |

> 💡 可以用 Excel 打开 CSV 检查。UTF-8、GB18030 编码都行，不用转码。

---

## 第 2 步：改个日期，点运行

1. VSCode 里打开 `run.py`
2. 右键 → **Run Python File in Terminal**
3. 终端里会出现一堆问题，**一直按回车就行**（全部用默认值），只改一个：

```
预测基准日 [2024-06-28]  格式 YYYY-MM-DD:  ← 这里输入你要的日期
```

4. 全部按完后会出现一个摘要表，输入 `y` 确认，就开始跑了。

> ⚠️ 整文件运行，别一行一行跑！

---

## 第 3 步：等着

你会看到三条进度条轮流跑：

```
⠋ 持续同调 ━━━━━━━━━━ 2345/5027  0:04:32 complete=...
⠋ 瓶颈匹配 ━━━━━━━━━━  456/4977  0:01:15 selected=...
⠋ 行情预测 ━━━━━━━━━━  234/ 530  0:00:08 complete=...
```

- 5000 支股票大概跑 **10–30 分钟**（取决于 CPU 核心数）
- 跑完终端里会直接显示准确率
- 中途关掉或死机也没事，**重新跑会自动接上**

---

## 第 4 步：看结果

所有结果在 `runs/你填的日期/` 文件夹里，打开就能看：

| 文件 | 里面是什么 |
|------|-----------|
| `runs/日期/outputs/report.txt` | 📄 **先看这个** — 文字版准确率总结 |
| `runs/日期/outputs/metrics.json` | 📊 逐天准确率数字 |
| `runs/日期/outputs/predictions.csv` | 📋 每支股票的详细预测（Excel 打开） |
| `runs/日期/outputs/selected_matches.csv` | 🔍 哪些历史点云被选为相似 |
| `runs/日期/artifacts.sqlite3` | 💾 全部实验数据（别删，删了就得重跑） |

---

## 改参数怎么改

在 `run.py` 的交互界面里，你可以改这些（回车不填=用默认值）：

| 参数 | 默认值 | 通俗解释 |
|------|--------|---------|
| 预测基准日 | `2024-06-28` | 站在哪天做预测 |
| 窗口大小 | `60` | 用多少天数据组成一个"指纹" |
| 看多少天历史 | `480` | 回溯多远的过去 |
| 相似度阈值 | `0.1` | 多像算"像"（越小越严） |
| 最像的几个 | `5` | 找几个最像的来投票 |
| 预测几天 | `5` | 预测未来几天的涨跌 |
| 并发数 | `0` | 同时跑几个进程/线程（0=自动，不用管） |
| 清空重算 | `n` | 换了行情数据时选 `y`，删掉旧结果从头算 |
| 授权就地清空 | `n` | 数据变了但你不想手动清库时选 `y` |

> 💡 不懂的参数全部回车跳过，默认值已经是跑过验证的最优组合。
>
> 💡 「瓶颈匹配并发数」现在用共享内存，多开进程不会成倍吃内存，16 核机器填 `0` 就会自动用 15 个进程。

---

## 换了行情数据怎么办

程序会对 `data/stock/` 里的 CSV **按内容算指纹**。只要文件内容变了（哪怕大小、修改时间没变），下次运行会直接报错拦住你，防止新旧数据混在一起。

遇到这种报错，两种处理方式：

| 情况 | 怎么做 |
|------|--------|
| 换了一批全新数据，旧结果不要了 | 「清空重算」选 `y` |
| 只是补了几天数据，想让程序自己收拾 | 「授权就地清空」选 `y` |
| 想保留旧结果对比 | 把「预测基准日」改成新日期（会生成新的 `runs/日期/` 文件夹） |

> 💡 命令行用户对应 `--reset` 和 `--force-rebuild` 两个开关。
>
> 💡 不选任何一个 = 保守模式：发现数据对不上就报错退出，绝不静默删你的数据。

---

## 出错了怎么办

| 你看到的 | 怎么办 |
|---------|--------|
| `行情目录中没有 CSV` | 去第 1 步，确认数据放对了位置（`data/stock/`） |
| `文件名应为六位代码加 SZ/SH` | 文件名格式不对，改名为 `000001.SZ.csv` 这样 |
| `缺少列 EventDate` | CSV 列名写错了，对照第 1 步的表改 |
| `No module named 'xxx'` | 跑一下 `setup_env.py`，它会自动修 |
| 跑到一半内存报错 | 把"持续同调并发数"调小（匹配阶段用共享内存，一般不是它的问题） |
| 预测阶段报错 | 你的 CSV 数据不够，基准日后面至少还要有 5 天数据 |
| `签名不匹配` / 拒绝在旧实验库继续 | 看上面"换了行情数据怎么办" |
| 上次强制关掉，再跑提示有锁文件 | 按提示选 `1`，会自动清锁并可终止残留进程 |

---

## 一句话总结

1. 跑 `setup_env.py`（仅第一次）
2. CSV 丢进 `data/stock/`
3. VSCode 右键跑 `run.py`，全回车只改日期
4. 去 `runs/日期/outputs/report.txt` 看结果

完。
